# Limit-Order-Book-Matching-Engine
A price-time priority matching engine supporting limit/market/cancel orders with O(1) cancels via hash-indexed iterators, processing 5M+ events/sec with full unit test coverage.
